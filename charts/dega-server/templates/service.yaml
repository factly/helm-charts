apiVersion: v1
kind: Service
metadata:
  name: {{ include "dega-server.fullname" . }}
  labels:
    {{- include "dega-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dega-server.selectorLabels" . | nindent 4 }}
