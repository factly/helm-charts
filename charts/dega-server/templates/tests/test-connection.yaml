apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dega-server.fullname" . }}-test-connection"
  labels:
    {{- include "dega-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dega-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
